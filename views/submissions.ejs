<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Submissions</title>
  </head>
  <body>
    <h1>View all of your submissions below</h1>
    <ul>
      <% if(submissions.length === 0){ %>
      <span>No submission so far</span>
      <% } else { for(let submission of submissions){ %>
      <li><%= submission %></li>
      <% }} %>
    </ul>
  </body>
</html>
